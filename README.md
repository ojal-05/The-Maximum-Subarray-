# The-Maximum-Subarray-
Maximum Subarray and Subsequence Sum (Java 8) A high-performance $O(n)$ solution that calculates the maximum contiguous sum using Kadane's Algorithm and the maximum non-contiguous sum using a Greedy approach. It efficiently handles edge cases, such as arrays containing only negative integers, by ensuring at least one element is selected.
